version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install -r requirements.txt

  build:
    commands:
      - echo "Skip execution - only scanning"

  post_build:
    commands:
      - echo "Done"

